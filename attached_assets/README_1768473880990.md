# YouthSchool MVP

🎓 **학교 문서 행정 AI 자동화 서비스**

AI 기반으로 가정통신문, 외부 교육 용역 계획서 등 학교 행정 문서를 자동으로 생성하는 서비스입니다.

---

## 📋 목차

1. [프로젝트 개요](#프로젝트-개요)
2. [주요 기능](#주요-기능)
3. [기술 스택](#기술-스택)
4. [설치 가이드](#설치-가이드)
5. [사용 방법](#사용-방법)
6. [API 문서](#api-문서)
7. [개발 가이드](#개발-가이드)

---

## 프로젝트 개요

### 목표
- 학교 행정 문서 작성 시간 80% 단축
- AI 기반 전문적인 문서 생성
- 사용자 맞춤 HWP 템플릿 지원

### MVP 범위
- **문서 유형**: 가정통신문, 외부 교육 용역 계획서
- **개발 기간**: 2025.01.15 ~ 2025.02.28 (6.5주)
- **목표**: 정부지원사업 선정 + 학교 5곳 시범 운영

---

## 주요 기능

### ✨ 핵심 기능
- 📄 **AI 문서 생성**: Claude AI 기반 자동 작성
- 🎯 **HWP 파일 처리**: 한글 파일 파싱 및 생성
- 📤 **사용자 템플릿**: 맞춤 양식 업로드 지원
- ⚡ **빠른 생성**: 평균 30초 이내 완료

---

## 기술 스택

### Backend
- **Python 3.11+**
- **FastAPI 0.109+**: REST API 서버
- **SQLAlchemy 2.0+**: ORM
- **pywin32 306**: HWP 파일 처리
- **Anthropic Claude API**: AI 텍스트 생성

### Frontend
- **HTML5 + CSS3**
- **Vanilla JavaScript**
- **Tailwind CSS**: 스타일링

### Database
- **SQLite** (MVP)
- **PostgreSQL** (프로덕션)

### DevOps
- **Docker + Docker Compose**
- **GitHub Actions**: CI/CD

---

## 설치 가이드

### 1. 사전 요구사항

```bash
# 필수
- Python 3.11+
- 한글과컴퓨터 한글 (HWP 처리용)
- Docker & Docker Compose (선택)

# Windows 환경 권장 (HWP 처리)
```

### 2. 프로젝트 클론

```bash
git clone https://github.com/yourusername/youthschool-mvp.git
cd youthschool-mvp
```

### 3. 환경 변수 설정

```bash
# .env 파일 생성
cp .env.example .env

# .env 파일 편집
# ANTHROPIC_API_KEY=sk-ant-api03-your-key-here
```

### 4-A. Docker로 실행 (추천)

```bash
# 서비스 시작
docker-compose up -d

# 로그 확인
docker-compose logs -f backend

# API 문서 접속
http://localhost:8000/docs
```

### 4-B. 로컬 실행

```bash
# 가상환경 생성
python -m venv venv
venv\Scripts\activate  # Windows
source venv/bin/activate  # Mac/Linux

# 의존성 설치
pip install -r backend/requirements.txt

# 서버 실행
cd backend
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# 프론트엔드 (별도 터미널)
cd frontend
python -m http.server 3000
```

---

## 사용 방법

### 1. 메인 페이지 접속
```
http://localhost:3000
```

### 2. 문서 유형 선택
- 가정통신문
- 외부 교육 용역 계획서

### 3. 정보 입력
- 제목, 내용 등 필수 정보 입력
- AI 생성 옵션 선택

### 4. 문서 생성
- AI가 자동으로 문서 작성
- 약 30초 소요

### 5. 다운로드
- HWP 파일 다운로드

---

## API 문서

### Swagger UI
```
http://localhost:8000/docs
```

### 주요 엔드포인트

#### 1. 문서 생성
```http
POST /api/v1/documents/generate
Content-Type: application/json

{
  "template_id": "template_001",
  "document_type": "가정통신문",
  "inputs": {
    "title": "겨울방학 안전 안내",
    "content": "..."
  }
}
```

#### 2. 문서 다운로드
```http
GET /api/v1/documents/{document_id}/download
```

#### 3. 템플릿 목록
```http
GET /api/v1/templates
```

#### 4. 템플릿 업로드
```http
POST /api/v1/templates/upload
Content-Type: multipart/form-data

file: (HWP 파일)
name: "내 템플릿"
```

---

## 개발 가이드

### 테스트 실행

```bash
# Unit 테스트
pytest tests/unit -v

# Integration 테스트
pytest tests/integration -v

# 커버리지 포함
pytest --cov=app --cov-report=html
```

### 코드 포맷팅

```bash
# Black (코드 포맷터)
black backend/app

# Flake8 (Linter)
flake8 backend/app

# 타입 체크
mypy backend/app
```

### Docker 개발 환경

```bash
# 백엔드 컨테이너 접속
docker-compose exec backend bash

# 로그 실시간 확인
docker-compose logs -f backend

# 데이터베이스 초기화
docker-compose exec backend alembic upgrade head
```

---

## 프로젝트 구조

```
youthschool-mvp/
├── backend/                    # FastAPI 백엔드
│   ├── app/
│   │   ├── main.py            # 메인 애플리케이션
│   │   ├── config.py          # 설정
│   │   ├── database.py        # DB 연결
│   │   ├── models/            # SQLAlchemy 모델
│   │   ├── routers/           # API 라우터
│   │   ├── services/          # 비즈니스 로직
│   │   └── schemas/           # Pydantic 스키마
│   ├── tests/                 # 테스트 코드
│   └── requirements.txt       # Python 의존성
├── frontend/                   # 프론트엔드
│   ├── index.html
│   ├── form.html
│   ├── result.html
│   ├── styles/
│   └── scripts/
├── docker-compose.yml         # Docker 설정
├── pytest.ini                 # pytest 설정
└── README.md
```

---

## 문제 해결

### HWP COM 오류
```bash
# 관리자 권한으로 Python 실행
# 한글 프로그램 재설치
```

### 포트 충돌
```bash
# 8000 포트가 사용 중일 경우
docker-compose down
netstat -ano | findstr :8000
taskkill /PID <PID> /F
```

### API 키 오류
```bash
# .env 파일에서 ANTHROPIC_API_KEY 확인
# https://console.anthropic.com 에서 키 재발급
```

---

## 라이선스

MIT License

---

## 연락처

**프로젝트 관리자**: Kim  
**GitHub**: https://github.com/yourusername/youthschool-mvp

---

## 다음 단계 (Phase 2)

- [ ] RAG 시스템 도입 (과거 문서 참고)
- [ ] Excel 예산표 자동 생성
- [ ] 사용자 인증 시스템
- [ ] 관리자 대시보드
- [ ] 모바일 앱
